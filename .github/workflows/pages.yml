name: Visual reference audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled to preserve GitHub Actions minutes"
